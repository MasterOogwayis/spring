# \u542F\u52A8\u524D\u52A0\u8F7D
server:
  port: 9104

spring:
  application:
    name: admin-server
  boot:
    admin:
#      oauth2: true
      routes:
        endpoints: env,metrics,trace,dump,jolokia,info,logfile,refresh,flyway,liquibase,heapdump,loggers,auditevents,configprops,hystrix.stream,activiti,mappings,shutdown
      username: ${security.user.name}       #These two are needed so that the conf
      password: ${security.user.password}   #can register at the protected server api
      client:
        metadata:
          user.name: ${security.user.name}          #These two are needed so that the server
          user.password:  ${security.user.password}
#  cloud:
#    config:
#      discovery:
#        enabled: true
#        service-id: bcpt-config-server
#      profile: cloud
#      failFast: true
#      # \u591A\u6587\u4EF6\u65E0\u6CD5\u4F7F\u7528\u9ED8\u8BA4\u6587\u4EF6\u540D\u4E86
#      name: monitor,instances,oauth2-api

eureka:
  server:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone:  http://${eureka.server.hostname}:${eureka.client.eureka-server-port}/eureka/
    eureka-server-port: 1111
  instance:
    #    hostname: localhost
    prefer-ip-address: true
#    ip-address: localhost
bcpt:
  server:
    version: V2017-12-28

security:
  user:
    name: user
    password: user
  basic:
    enabled: false


