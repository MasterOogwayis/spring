eureka:
  server:
    hostname: ${REGISTER_ADDRESS}
  client:
    serviceUrl:
      defaultZone:  http://${eureka.server.hostname}:${eureka.client.eureka-server-port}/eureka/
    eureka-server-port: 11111
    enabled: true
  instance:
    prefer-ip-address: true