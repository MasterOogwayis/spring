spring:
  cloud:
    nacos:
      config:
        server-addr: 192.168.1.13:8848
        file-extension: yml
        # default
        group: DEFAULT_GROUP
        shared-configs:
          - dataId: alt-navigation-api-dev.yml
            refresh: true
          - dataId: redis.yml
            refresh: true